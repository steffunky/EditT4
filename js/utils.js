// Generated by CoffeeScript 1.9.3
(function() {
  var slice = [].slice;

  window.Utils = (function() {
    function Utils() {}

    // Return a new copy of the hash in argument
    Utils.copyHash = function(hash) {
      var i, k, len, new_hash;
      new_hash = {};
      for (i = 0, len = hash.length; i < len; i++) {
        k = hash[i];
        new_hash[k] = hash[k];
      }
      return new_hash;
    };

    // Avoid bug and set the mouse up after a while
    Utils.mouseTableUp = function(table) {
      return setTimeout((function(_this) {
        return function() {
          return table.setMouseDown(false);
        };
      })(this), 50);
    };

    // Merge all hashes and return a new one
    Utils.merge = function() {
      var hash, hash_final, hashs, i, key, len, value;
      hashs = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      hash_final = {};
      for (i = 0, len = hashs.length; i < len; i++) {
        hash = hashs[i];
        for (key in hash) {
          value = hash[key];
          hash_final[key] = value;
        }
      }
      return hash_final;
    };

    // Return true if hash1 and hash2 have the same properties and the same values
    Utils.hashEqual = function(hash1, hash2) {
      var key, value;
      for (key in hash1) {
        value = hash1[key];
        if (!((hash2[key] != null) && hash2[key] === value)) {
          return false;
        }
      }
      return true;
    };

    return Utils;

  })();

}).call(this);
